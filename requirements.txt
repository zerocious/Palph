aiogram>=3.26,<4
# aiohttp импортируется напрямую в api.py (HTTP API для desktop-клиента);
# диапазон совпадает с тем, что допускает aiogram — новой зависимости нет.
aiohttp>=3.9,<3.15
aiosqlite>=0.22,<1
pytz>=2024.1
python-dotenv>=1.0,<2
