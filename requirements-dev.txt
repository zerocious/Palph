# Dev-only dependencies — не нужны в production-образе.
# Установка: pip install -r requirements-dev.txt
-r requirements.txt
# pytest 9.0.3 — фикс CVE-2025-71176 (был в pytest 8.x); pytest-asyncio
# обновлён на 1.x ради совместимости с pytest 9.
pytest>=9.0.3,<10
pytest-asyncio>=1.0,<2
# Analysis notebooks (analysis/*.ipynb).
pandas>=2.0,<3
matplotlib>=3.8,<4
jupyter>=1.0,<2
# Asset build script only (scripts/build_pet_assets.py) — pin for CVE tracking.
# Нижняя граница 12.3: в 11.x и старше висят 25 уязвимостей (PYSEC-2026-2253,
# -2255, -2256, -3451, -3453, -3454, -3493..-3496 и др.), из-за которых
# падал шаг «Audit dev deps» в .github/workflows/security.yml.
Pillow>=12.3,<13
